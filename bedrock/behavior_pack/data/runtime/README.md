# Runtime Payload Folder

Place exported runtime files here during integration testing:

- `runtime_rules.json`
- `runtime_conditions.json`
- `runtime_assets.json`
- `manifest.json` (optional)

This folder is intentionally empty in repository source; files are generated by Python export and copied here.
