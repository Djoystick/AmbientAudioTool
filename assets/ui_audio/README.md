# UI Audio Assets

This directory is intentionally a placeholder in the public repository.

Put your own GUI background track file here:

- `background_loop.mp3`

The GUI music toggle reads this exact file path:

- `assets/ui_audio/background_loop.mp3`
